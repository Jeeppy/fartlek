!!!
%html.h-full.bg-gray-950{ lang: "fr" }
  %head
    %meta{ charset: "utf-8" }
    %meta{ name: "viewport", content: "width=device-width, initial-scale=1" }
    %title Fartlek — #{content_for?(:title) ? yield(:title) : "Carnet d'entraînement"}
    %meta{ name: "turbo-cache-control", content: "no-preview" }
    = csrf_meta_tags
    = csp_meta_tag
    = stylesheet_link_tag "tailwind", "data-turbo-track": "reload"
    = stylesheet_link_tag "application", "data-turbo-track": "reload"
    = javascript_importmap_tags

  %body.h-full.bg-gray-950.text-gray-100
    - if user_signed_in?
      .min-h-full
        = render "shared/navbar"
        %main.mx-auto.max-w-7xl.px-4.py-8{ class: "sm:px-6 lg:px-8" }
          = render "shared/flash"
          = yield
    - else
      %main.flex.min-h-full.items-center.justify-center.px-4.py-12
        = render "shared/flash"
        = yield
